<% provide(:title, 'All users') %>

<div class="row">
	<div class="col-md-6 col-md-offset-3 box">
		<h1>All users</h1>
		<hr>
		<div class="center">
			<%= will_paginate%>	
		</div>
		
		<ul class="users ">
		<%= render @users %>
		</ul>
		<div class="center">
			<%= will_paginate%>	
		</div>
	</div>
</div>
